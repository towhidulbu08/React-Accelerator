<==>L-3.16 Passing Data Deeply with Context - Introduction to Context API<==>

Usually, you will pass information from a parent component to a child component via props. But passing props can become verbose and inconvenient if you have to pass them through many components in the middle, or if many components in your app need the same information. Context lets the parent component make some information available to any component in the tree below it—no matter how deep—without passing it explicitly through props.

=>You will learn:
1.What “prop drilling” is
2.How to replace repetitive prop passing with context
3.Common use cases for context
4.Common alternatives to context

=>The problem with passing props: 
Passing props is a great way to explicitly pipe data through your UI tree to the components that use it.

But passing props can become verbose and inconvenient when you need to pass some prop deeply through the tree, or if many components need the same prop. The nearest common ancestor could be far removed from the components that need data, and lifting state up that high can lead to a situation called “prop drilling”.

Context: an alternative to passing props 
Context lets a parent component provide data to the entire tree below it. There are many uses for context.

You will do it in three steps:

i.Create a context. (You can call it LevelContext, since it’s for the heading level.)
ii.Use that context from the component that needs the data. (Heading will use LevelContext.)
iii.Provide that context from the component that specifies the data. (Section will provide LevelContext.)
Context lets a parent—even a distant one!—provide some data to the entire tree inside of it.

Note:
i.The only argument to createContext is the default value
ii.useContext is a Hook. Just like useState and useReducer, you can only call a Hook immediately inside a React component (not inside loops or conditions). useContext tells React that the Heading component wants to read the LevelContext.