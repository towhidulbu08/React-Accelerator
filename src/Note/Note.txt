<=>L-5.1:React Router v6 Crash Course<=>

1.Client Side Routing:
2.Single Page Application:
3.React Router Dom:
     i.Root Route
     ii.nested Routing
     iii.loading Data using loader and useLoader
     iv. Url params in loaders
     v. updating data
     vi. deleting data
     vii. index  route


==> Hooks:
1.useRouteError:
2.useNavigation:


3.useNavigate: The useNavigate hook returns a function that lets you navigate programmatically.
        
         The navigate function has two signatures:

     i.Either pass a To value (same type as <Link to>) with an optional second options argument (similar to the props you can pass to <Link>), or
     ii.Pass the delta you want to go in the history stack. For example, navigate(-1) is equivalent to hitting the back button


4.useLoaderData:
5.useFetcher:


 Note:In JavaScript, the confirm() function returns a boolean: true if the user clicks "OK" and false if the user clicks "Cancel."           